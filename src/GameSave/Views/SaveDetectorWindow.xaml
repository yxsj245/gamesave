<Window
    x:Class="GameSave.Views.SaveDetectorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="🔍 存档目录探测">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,12">
            <TextBlock Text="🔍 存档目录探测中..." 
                       FontSize="18" 
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="StatusText"
                       Text="请在游戏中进行存档操作，工具会自动捕获疑似存档目录" 
                       FontSize="13" 
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- 进度指示 -->
        <ProgressBar x:Name="DetectingProgress" 
                     Grid.Row="1" 
                     IsIndeterminate="True" 
                     Margin="0,0,0,12"/>

        <!-- 存档目录列表 -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,12">
            <StackPanel x:Name="DirectoryList" Spacing="4">
                <!-- 空状态提示 -->
                <TextBlock x:Name="EmptyHint"
                           Text="暂未发现任何疑似存档目录，请在游戏中执行存档操作..."
                           FontSize="13"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,40,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部操作区 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="SelectedCountText" 
                       Grid.Column="0"
                       Text="未选择任何目录" 
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    Content="取消"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>

            <Button x:Name="ConfirmButton"
                    Grid.Column="2"
                    Content="确认选择"
                    Style="{StaticResource AccentButtonStyle}"
                    IsEnabled="False"
                    Click="ConfirmButton_Click"/>
        </Grid>
    </Grid>
</Window>
