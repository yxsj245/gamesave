<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="GameSave.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="700">
            <TextBlock Text="设置" FontSize="28" FontWeight="Bold" Margin="0,0,0,8"/>

            <!-- 外观设置 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="外观" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="选择应用的显示主题" FontSize="13" Foreground="Gray"/>
                    
                    <RadioButtons x:Name="ThemeRadioButtons"
                                  SelectedIndex="{x:Bind ViewModel.SelectedThemeIndex, Mode=TwoWay}"
                                  MaxColumns="3">
                        <RadioButton Content="跟随系统" Tag="System"/>
                        <RadioButton Content="浅色模式" Tag="Light"/>
                        <RadioButton Content="深色模式" Tag="Dark"/>
                    </RadioButtons>
                </StackPanel>
            </Border>

            <!-- 工作目录设置 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock Text="工作目录" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="所有游戏存档备份文件将保存在此目录下" FontSize="13" Foreground="Gray"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="WorkDirTextBox" 
                                 Grid.Column="0"
                                 IsReadOnly="True"
                                 Text="{x:Bind ViewModel.WorkDirectory, Mode=OneWay}"/>
                        <Button Grid.Column="1" 
                                Content="更改" 
                                Margin="8,0,0,0"
                                Click="ChangeWorkDir_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 云端存储配置 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="云端存储" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="配置云端存储服务商，实现存档自动同步" FontSize="13" Foreground="Gray" Margin="0,4,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" 
                                Content="添加配置"
                                VerticalAlignment="Center"
                                Click="AddCloudConfig_Click">
                            <Button.Resources>
                                <FontIcon x:Key="Icon" Glyph="&#xE710;"/>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <!-- 已有配置列表 -->
                    <ListView x:Name="CloudConfigListView"
                              ItemsSource="{x:Bind ViewModel.CloudConfigs, Mode=OneWay}"
                              SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8,12" Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 图标 -->
                                    <FontIcon Grid.Column="0" Glyph="&#xE753;" 
                                              FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                              VerticalAlignment="Center" Margin="0,0,12,0"/>

                                    <!-- 配置信息 -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}" FontSize="14" FontWeight="SemiBold"/>
                                        <TextBlock FontSize="12" Foreground="Gray">
                                            <Run Text="阿里云 OSS · "/>
                                            <Run Text="{Binding BucketName}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- 删除按钮 -->
                                    <Button Grid.Column="2" 
                                            VerticalAlignment="Center"
                                            Click="DeleteCloudConfig_Click"
                                            Tag="{Binding}"
                                            Background="Transparent"
                                            BorderThickness="0">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="Gray"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- 空列表提示 -->
                    <TextBlock x:Name="NoConfigHint"
                               Text="暂未配置云端存储，点击上方「添加配置」开始使用"
                               FontSize="13" Foreground="Gray" 
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 关于信息 -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="关于" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="GameSave Manager" FontSize="14"/>
                    <TextBlock Text="一款简洁的游戏存档备份与恢复工具" FontSize="13" Foreground="Gray"/>
                    <TextBlock Text="版本: 1.0.0" FontSize="12" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
